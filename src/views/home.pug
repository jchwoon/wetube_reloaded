extends base.pug
include mixins/video.pug

block head
    div.video-grid
        each video in videos
            +video(video)
            +video(video)
            +video(video)
            +video(video)
            +video(video)
            +video(video)
        else
            span.empty__message No videos found 