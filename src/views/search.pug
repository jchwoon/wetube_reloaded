extends base.pug
include mixins/video

block head
    form(method="GET").search__form
        input(type="text",name="keyword",placeholder="Search Video that You Want" )
        button
            i.fas.fa-search
    div.video-grid
        each video in videos 
            +video(video)
        else 
            span.empty__message No videos found 